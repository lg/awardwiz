{
  "name": "Playwright",
  "id": "playwright",
  "version": "1.0.0",
  "description": "A feature that installs Playwright and it's dependencies. Note that you need to have npm and an X server configured.",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "The version of Playwright to install (ex. '1.31.1'). Use 'latest' to get the latest version."
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ],
  "containerEnv": {
    "PLAYWRIGHT_BROWSERS_PATH": "/ms-playwright"
  }
}